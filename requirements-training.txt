# Exact package versions used to fine-tune the models in the paper.
#
# This stack MUST live in its own virtual environment. axolotl pins torch and
# transformers versions that conflict with the analysis environment's vLLM
# install; combining them produces an environment that cannot run either.
#
#   python -m venv .venv-training && source .venv-training/bin/activate
#   pip install -r requirements-training.txt
#
# Python 3.11.6, 4x NVIDIA A40, CUDA 12.6.
#
# flash-attn builds against your installed torch and CUDA toolkit, so it must be
# installed after torch and generally needs `--no-build-isolation`. If the build
# fails, drop `flash_attention: true` from the axolotl configs -- it affects
# speed and memory, not results.

axolotl==0.13.2
torch==2.10.0
transformers==4.57.6
accelerate==1.12.0
deepspeed==0.18.2
bitsandbytes==0.49.1
peft==0.18.1
trl==0.27.0
datasets==4.5.0
flash-attn==2.8.3
