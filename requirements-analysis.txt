# Exact package versions used to produce the paper's results.
#
# This is a reproduction lock, not the everyday install path. For normal use
# prefer `pip install -e ".[analysis]"`, which allows newer versions. Use this
# file when you need to match the paper exactly, or when a newer release has
# changed behavior.
#
#   python -m venv .venv-analysis && source .venv-analysis/bin/activate
#   pip install -r requirements-analysis.txt
#   pip install -e . --no-deps
#
# Python 3.11.7. Do NOT install this alongside requirements-training.txt --
# they are separate environments by design (see "Environments" in README.md).
#
# Version-sensitive entries are flagged below; see "Known version issues" in
# README.md for details.

rdkit==2025.9.6              # canonical SMILES + bond perception: changes results
scikit-learn==1.8.0          # train_test_split(random_state=42): defines the QM9 split
torch-geometric==2.7.0       # required to unpickle the GEOM archives
numpy==2.2.6                 # RandomState semantics; 1.x is a breaking change
matplotlib==3.10.8           # figure rendering: affects byte-identical output
seaborn==0.13.2
scipy==1.17.1
pandas==3.0.2
joblib==1.5.3
tqdm==4.67.3
transformers==4.57.6
torch==2.10.0

# vLLM was built against CUDA 12.6 (installed as 0.18.0+cu126). PyPI serves a
# default build under the plain version; pick the wheel matching your CUDA
# toolkit. Only needed for batch inference, not for evaluating cached results.
vllm==0.18.0
